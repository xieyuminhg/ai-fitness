# 前端开发详解 — 从零搭建 AI Fitness 助手

> 写给新手小白：本文档逐行解释这个前端项目是怎么设计和实现的。
>
> 技术栈：HTML（结构） + CSS（样式） + JavaScript（逻辑），无框架。

---

## 一、设计思路：先想清楚再写代码

### 1.1 这个应用要干什么？

一个 AI 健身助手，用户可以：
1. 注册账号 / 登录
2. 创建对话
3. 给 AI 发消息，收到回复
4. 查看历史对话
5. 删除对话

### 1.2 页面长什么样？（先画草稿）

```
未登录状态（居中卡片）：
┌─────────────────────────┐
│    AI Fitness 助手      │
│   你的专属智能健身教练    │
│                         │
│   [登录]  [注册]        │
│                         │
│   用户名: [________]    │
│   密码:   [________]    │
│   [登 录]               │
└─────────────────────────┘

登录后状态（双栏布局）：
┌───────────┬───────────────────────┐
│ 侧边栏     │  聊天区域              │
│ 280px     │  剩余宽度              │
│           │                       │
│ AI Fitness│  ┌──────────────┐     │
│ [+ 新对话] │  │ AI 回复气泡   │     │
│ ──────────│  └──────────────┘     │
│ · 会话1   │  ┌──────────────┐     │
│ · 会话2   │  │     用户气泡 ─┤    │
│           │  └──────────────┘     │
│           │                       │
│ 用户名     │  ┌───────┬────┐      │
│ [登出]    │  │输入框  │发送│      │
└───────────┘  └───────┴────┘      │
```

### 1.3 文件怎么组织？

```
frontend/
  index.html      ← 唯一的页面（单页应用）
  css/
    style.css     ← 全部样式放一个文件
  js/
    api.js        ← 负责和后端通信
    app.js        ← 负责页面逻辑
```

**为什么这样分？**

| 文件 | 管什么 | 打个比方 |
|------|--------|---------|
| `index.html` | 结构：有什么元素 | 房子的钢筋骨架 |
| `style.css` | 样式：长什么样 | 装修和配色 |
| `api.js` | 数据：怎么跟后端说话 | 快递员，负责收发 |
| `app.js` | 逻辑：用户操作怎么响应 | 大脑，做决策 |

---

## 二、HTML 详解 — 页面的骨架

### 2.1 基本结构

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AI Fitness 助手</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <!-- 页面内容 -->
    <script src="js/api.js"></script>
    <script src="js/app.js"></script>
</body>
</html>
```

几个关键点：
- `lang="zh-CN"` — 告诉浏览器这是中文页面
- `viewport` — 手机也能正常显示
- `<script>` 放最后 — 等 HTML 加载完再执行 JS

### 2.2 登录/注册区域

```html
<div id="auth-page">
    <div class="auth-card">
        <h1>AI Fitness 助手</h1>
        ...
    </div>
</div>
```

- `id="auth-page"` 是这个区域的唯一标识，JS 通过它来显示/隐藏
- 登录和注册用两张表单 `<form>`，通过切换标签来决定显示哪张

**标签切换的思路**：两个按钮 `[登录] [注册]`，点哪个就给谁加 `.active` CSS 类，同时显示对应的表单、隐藏另一个。这样不需要跳转页面。

### 2.3 主应用区域（双栏布局）

```html
<div id="app-page" class="app-layout">
    <aside id="sidebar" class="sidebar">   <!-- 左侧 -->
        ...
    </aside>
    <main class="chat-area">               <!-- 右侧 -->
        ...
    </main>
</div>
```

布局用 `display: flex` 把两个区域并排：

```css
.app-layout { display: flex; height: 100vh; }   /* 占满整个窗口高度 */
.sidebar { width: 280px; }                       /* 侧边栏固定 280px */
.chat-area { flex: 1; }                          /* 聊天区占剩余空间 */
```

### 2.4 聊天区域的三种状态

聊天区有三种显示状态，通过 JS 控制显隐：

| 状态 | 显示内容 | 什么时候出现 |
|------|---------|-------------|
| 占位 | 💪 图标 + 提示文字 | 刚登录，还没选任何会话 |
| 消息列表 | 对话气泡 | 选中了一个会话 |
| 输入区域 | 文本框 + 发送按钮 | 和消息列表同时出现 |

HTML 中三个区域都有，初始都隐藏，JS 根据需要 `display: none/flex`。

---

## 三、CSS 详解 — 页面怎么样

### 3.1 CSS 变量 — 统一管理颜色

```css
:root {
    --primary: #4f46e5;        /* 主色：紫色 */
    --primary-hover: #4338ca;  /* 鼠标悬停：深紫 */
    --bg: #f8fafc;             /* 背景：浅灰 */
    --text: #1e293b;           /* 文字：深灰 */
    ...
}
```

**为什么用变量？** 假如以后想换主题色，只改 `--primary` 一处就行，不用满文件搜索 `#4f46e5`。

使用方式：`color: var(--primary);`

### 3.2 登录页 — 居中卡片 + 渐变背景

```css
#auth-page {
    display: flex;
    align-items: center;       /* 垂直居中 */
    justify-content: center;   /* 水平居中 */
    height: 100vh;             /* 占满整个窗口 */
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}
```

`linear-gradient` 创建从左下到右上的紫色渐变背景。

卡片本身：
```css
.auth-card {
    background: white;
    border-radius: 16px;       /* 圆角 */
    padding: 40px;
    width: 400px;
    box-shadow: 0 20px 60px rgba(0,0,0,0.2);  /* 阴影让它浮起来 */
}
```

### 3.3 标签切换样式

```css
.auth-tab {
    border-bottom: 2px solid transparent;  /* 默认透明底边 */
}
.auth-tab.active {
    color: var(--primary);
    border-bottom-color: var(--primary);   /* 激活时紫色底边 */
}
```

核心思路：`.active` 类控制高亮，JS 切换类名就行。

### 3.4 侧边栏 — 深色主题

```css
.sidebar {
    width: 280px;
    background: #1e1b4b;       /* 深紫背景 */
    display: flex;
    flex-direction: column;    /* 内容纵向排列 */
}
```

Flex 纵向排列的好处：头部固定、列表自动撑满、底部固定。

```css
.conv-list { flex: 1; overflow-y: auto; }  /* 列表区占满剩余空间，超出滚动 */
.sidebar-footer { border-top: 1px solid ...; }  /* 底部固定在最后 */
```

### 3.5 删除按钮 — hover 才显示

```css
.conv-item .conv-del { opacity: 0; }         /* 默认透明 */
.conv-item:hover .conv-del { opacity: 1; }   /* 鼠标悬停时显示 */
```

### 3.6 消息气泡 — 左右分布

```css
.message.user { align-self: flex-end; }       /* 用户消息靠右 */
.message.assistant { align-self: flex-start; } /* AI 消息靠左 */

/* 用户气泡：紫色 */
.message.user .msg-content { background: var(--primary); color: white; }

/* AI 气泡：灰色 */
.message.assistant .msg-content { background: #f1f5f9; }

/* 气泡尾巴效果：右下角/左下角收小 */
.message.user .msg-content { border-bottom-right-radius: 4px; }
.message.assistant .msg-content { border-bottom-left-radius: 4px; }
```

### 3.7 思考中动画

```css
@keyframes thinking {
    0%, 80%, 100% { transform: scale(0.4); opacity: 0.3; }  /* 缩小+半透明 */
    40% { transform: scale(1); opacity: 1; }                  /* 正常大小 */
}
```

三个圆点，每个有不同的 `animation-delay`，形成波浪效果。

### 3.8 Toast 提示 — 渐入渐出

```css
.toast {
    position: fixed;          /* 固定定位，不随滚动 */
    bottom: 40px;
    left: 50%;
    transform: translateX(-50%);  /* 真正的水平居中 */
    opacity: 0;
    transition: opacity 0.3s;     /* 淡入淡出 */
}
.toast.show { opacity: 1; }
```

---

## 四、JavaScript 详解（一）— api.js 跟后端通信

### 4.1 核心：request 函数

```javascript
const API_BASE = 'http://127.0.0.1:8000';

async function request(path, { method = 'GET', body, auth = false } = {}) {
    // 1. 设置请求头
    const headers = { 'Content-Type': 'application/json' };

    // 2. 如果需要认证，从 localStorage 取 token 附上
    if (auth) {
        const token = localStorage.getItem('token');
        if (token) headers['Authorization'] = `Bearer ${token}`;
    }

    // 3. 发送请求
    const res = await fetch(API_BASE + path, {
        method,
        headers,
        body: body ? JSON.stringify(body) : undefined,
    });

    // 4. 解析响应
    const json = await res.json();

    // 5. 如果 401，说明登录过期，强制登出
    if (json.code === 401 && auth) {
        localStorage.removeItem('token');
        localStorage.removeItem('user');
        window.location.reload();
        throw new Error('登录已过期');
    }

    // 6. 业务错误抛出异常
    if (json.code !== 200) {
        throw new Error(json.message || '请求失败');
    }

    return json;
}
```

**关键概念解释：**

| 概念 | 解释 |
|------|------|
| `async/await` | 让异步代码看起来像同步代码。`await` = "等这个操作完成再往下走" |
| `fetch()` | 浏览器内置的发 HTTP 请求的方法 |
| `JSON.stringify()` | 把 JS 对象变成 JSON 字符串（发请求用） |
| `res.json()` | 把服务器返回的 JSON 字符串变回 JS 对象 |
| `localStorage` | 浏览器本地存储，关掉页面数据还在 |
| `throw new Error()` | 抛出异常，调用方用 `try/catch` 接住 |
| 解构参数 `{} = {}` | 函数参数默认值的写法，不传就空对象 |

### 4.2 具体 API 函数

每个函数都很简单 — 就是调用 `request()` 并传入具体参数：

```javascript
function apiLogin(username, password) {
    return request('/api/auth/login', {
        method: 'POST',
        body: { username, password },
    });
}
```

所有认证接口 `auth: false`（默认），所有需要登录的接口 `auth: true`。

---

## 五、JavaScript 详解（二）— app.js 页面逻辑

### 5.1 全局状态 — 一个对象管理所有数据

```javascript
const state = {
    token: localStorage.getItem('token') || null,   // JWT
    user: JSON.parse(localStorage.getItem('user') || 'null'),
    conversations: [],    // 会话列表
    currentConvId: null,  // 当前选中的会话 ID
    messages: [],         // 当前会话的消息
};
```

**为什么用全局状态对象？** 所有函数都读写同一个 `state`，数据流向清晰：改了 state → 调 render 函数 → 页面自动更新。这就是最简版的 "状态管理"。

### 5.2 页面切换逻辑

```
用户打开页面
  ├─ localStorage 里有 token？ → 显示主应用
  └─ 没有 token？            → 显示登录页
```

```javascript
if (state.token && state.user) {
    showAppPage();   // 已登录
} else {
    showAuthPage();  // 未登录
}
```

### 5.3 登录流程

```
用户点 [登录]
  → 禁用按钮，显示"登录中..."
  → 调用 apiLogin(username, password)
     ├─ 成功 → 拿到 token → 存 localStorage → 切换到主界面
     └─ 失败 → 显示错误提示 → 恢复按钮
```

关键代码：
```javascript
btn.disabled = true;           // 防止重复点击
btn.textContent = '登录中...';  // 反馈给用户
try {
    const res = await apiLogin(username, password);
    localStorage.setItem('token', res.data.token);  // 持久化存储
    showAppPage();  // 切换页面
} catch (err) {
    errorEl.textContent = err.message;  // 显示错误
} finally {
    btn.disabled = false;  // 不管成功失败，恢复按钮
}
```

`try/catch/finally` 模式要点：
- `try` — 可能出错的代码
- `catch` — 出错时执行（如网络不通、密码错误）
- `finally` — 不管成功失败都执行（恢复按钮状态）

### 5.4 会话列表

**加载会话：**
```javascript
async function loadConversations() {
    const res = await apiGetConversations();
    state.conversations = res.data.list;  // 更新状态
    renderConvList();                      // 渲染到页面
}
```

**渲染列表：** 用 `Array.map()` 把数据数组变成 HTML 字符串：
```javascript
function renderConvList() {
    const html = state.conversations.map(c =>
        `<div class="conv-item${c.id === state.currentConvId ? ' active' : ''}">
            <span>${c.title}</span>
            <button class="conv-del">×</button>
        </div>`
    ).join('');
    document.getElementById('conv-list').innerHTML = html;
}
```

**事件绑定 — 事件代理思路：** 先 `innerHTML` 写好 HTML，再 `querySelectorAll` 给每个元素绑事件。这是最简单的纯 JS 做法。

### 5.5 消息发送 — 最复杂的流程

```
用户输入文字 → 按 Enter
  → 立刻把用户消息追加到 messages 数组
  → 重新渲染消息列表（用户立即看到自己发的消息）
  → 显示"思考中"动画
  → 调用 apiSendMessage()
     ├─ 成功 → 隐藏思考动画 → 追加 AI 回复 → 重新渲染
     └─ 失败 → 隐藏思考动画 → Toast 提示
  → 恢复输入框和按钮
```

关键设计：**先显示用户消息，再等 AI 回复**。这样用户打字后立刻看到自己的消息，不用等。

```javascript
async function sendMessage() {
    // 1. 立即显示用户消息
    state.messages.push({
        role: 'user',
        content: content,
        created_at: new Date().toISOString(),
    });
    renderMessages();
    showThinking();  // 显示三个跳动圆点

    // 2. 等 AI 回复
    try {
        const res = await apiSendMessage(state.currentConvId, content);
        hideThinking();
        state.messages.push({
            role: 'assistant',
            content: res.data.content,
            created_at: res.data.created_at,
        });
        renderMessages();
    } catch (err) {
        hideThinking();
        showToast('发送失败: ' + err.message);
    }
}
```

### 5.6 Enter 发送 + Shift+Enter 换行

```javascript
input.addEventListener('keydown', (e) => {
    if (e.key === 'Enter' && !e.shiftKey) {
        e.preventDefault();   // 阻止默认换行
        sendMessage();        // 发送
    }
    // 如果按着 Shift，就走默认行为（换行），不拦截
});
```

### 5.7 自动增高输入框

```javascript
textarea.addEventListener('input', function () {
    this.style.height = 'auto';                           // 先重置
    this.style.height = Math.min(this.scrollHeight, 120) + 'px';  // 再撑开，上限 120px
});
```

### 5.8 Markdown 渲染

AI 回复可能是这样的：
```markdown
我建议你试试以下方案：

1. **有氧运动**：每周 3 次，每次 30 分钟
2. **力量训练**：可以做 `深蹲`、`俯卧撑`

> 记得热身！
```

需要转成带样式的 HTML。原理：用正则表达式匹配 Markdown 语法，替换成 HTML 标签。

```javascript
function renderMarkdown(text) {
    let html = escapeHtml(text);  // 先把所有 HTML 特殊字符转义，防 XSS

    // **粗体** → <strong>粗体</strong>
    html = html.replace(/\*\*(.+?)\*\*/g, '<strong>$1</strong>');

    // `代码` → <code>代码</code>
    html = html.replace(/`([^`]+)`/g, '<code>$1</code>');

    // ```代码块``` → <pre><code>...</code></pre>
    html = html.replace(/```(\w*)\n?([\s\S]*?)```/g,
        (_, lang, code) => `<pre><code>${escapeHtml(code.trim())}</code></pre>`
    );

    // 以此类推...
    return html;
}
```

**正则表达式快速理解：**
| 正则 | 含义 |
|------|------|
| `\*\*(.+?)\*\*` | 匹配 `**任意文字**` |
| `` `([^`]+)` `` | 匹配 `` `任意文字` `` |
| `$1` | 表示第一个括号里匹配到的内容 |

### 5.9 401 自动登出

在 `api.js` 的 `request()` 函数里，任何需要认证的请求如果返回 401，说明 token 过期了：

```javascript
if (json.code === 401 && auth) {
    localStorage.removeItem('token');
    localStorage.removeItem('user');
    window.location.reload();  // 刷新页面，回到登录
}
```

用户完全不需要手动登出，过期自动跳转。

---

## 六、消息流程图（完整链路）

```
用户打字 → 按 Enter
  │
  ├→ sendMessage()
  │    ├→ 用户消息 push 进 state.messages
  │    ├→ renderMessages() → 页面显示用户气泡
  │    ├→ showThinking() → 显示三点动画
  │    │
  │    ├→ apiSendMessage(convId, content)
  │    │    └→ request('/api/conversations/1/messages', { method:'POST', body:{content}, auth:true })
  │    │         └→ fetch('http://127.0.0.1:8000/api/conversations/1/messages', {
  │    │               headers: { Authorization: 'Bearer xxx' },
  │    │               body: '{"content":"你好"}'
  │    │            })
  │    │              │
  │    │              ├─ 后端处理 ─────────────────────
  │    │              │  1. 验证 JWT
  │    │              │  2. 保存用户消息到 DB
  │    │              │  3. 构建上下文（历史消息 + system prompt）
  │    │              │  4. 调用 Anthropic API
  │    │              │  5. 保存 AI 回复到 DB
  │    │              │  6. 返回 AI 回复
  │    │              └────────────────────────────
  │    │
  │    ├→ hideThinking()
  │    ├→ AI 回复 push 进 state.messages
  │    └→ renderMessages() → 页面显示 AI 气泡
  │
  └→ 恢复输入框，自动聚焦
```

---

## 七、常见问题

### 7.1 为什么 localStorage 存 token 而不是 cookie？

- `localStorage` 操作简单：`setItem` / `getItem` / `removeItem`
- 纯前端项目，没有后端渲染页面，不需要 cookie
- 每次请求手动在 header 里带 `Authorization: Bearer xxx`

### 7.2 什么是单页应用（SPA）？

整个网站只有一个 HTML 文件，页面切换通过 JS 显示/隐藏不同区域来实现，不刷新浏览器。

本项目的两个"页面"就是两个 `<div>`：
- `#auth-page` — 登录注册
- `#app-page` — 主应用

登录成功后，`showAppPage()` 隐藏前者、显示后者。

### 7.3 为什么消息气泡会自己滚到底部？

```javascript
el.scrollTop = el.scrollHeight;
```

`scrollTop` = 滚动条当前位置，`scrollHeight` = 内容总高度。设为相等即滚到底。

### 7.4 为什么用户消息和 AI 回复分开 push？

因为后端只返回 AI 回复（`SendMessageRequest` 接口设计）。用户消息前端已持有，直接本地追加即可，不用再调接口获取。

### 7.5 为什么不在 HTML 里写 onclick？

```html
<!-- ❌ 不推荐 -->
<button onclick="sendMessage()">发送</button>

<!-- ✅ 推荐 -->
<button id="btn-send">发送</button>
```

```javascript
document.getElementById('btn-send').addEventListener('click', sendMessage);
```

好处：
- HTML 只管结构，不管行为（关注点分离）
- `addEventListener` 可以绑多个事件
- 代码更容易维护

---

## 八、如何自己测试

### 8.1 启动后端

```powershell
cd D:\zhuanye\pycharm\project\demo1
.venv\Scripts\activate
uvicorn backend.main:app --reload
```

确认 `http://127.0.0.1:8000/docs` 能打开。

### 8.2 打开前端

直接在文件管理器里双击 `frontend/index.html`，或用 VS Code 的 Live Server 插件打开。

### 8.3 测试流程

1. 打开页面 → 看到登录表单
2. 点"注册"，输入用户名和密码，点注册
3. 自动进入主界面，左侧侧边栏为空
4. 点"+ 新对话"→ 创建第一个会话
5. 在输入框打字 → 按 Enter 发送
6. 看到自己的消息气泡，然后看到三点跳动动画
7. AI 回复出现（带 Markdown 渲染）
8. 再点"+ 新对话"→ 创建第二个会话 → 发送消息
9. 在左侧切换会话 → 看到各自的消息
10. hover 会话项 → 出现 × → 点删除 → 确认 → 会话消失
11. 点"登出"→ 回到登录页

### 8.4 打开浏览器开发者工具

按 `F12`，切换到 **Console** 标签，可以看到 JS 的错误信息。切换到 **Network** 标签，可以看到每个 API 请求的详情。

### 8.5 如果页面没反应

在 Console 里看有没有红色报错。常见原因：
- 后端没启动 → 看到 `Failed to fetch` 错误
- 端口被占 → 换端口（改 `api.js` 的 `API_BASE`）
- 跨域问题 → 检查后端 CORS 配置

---

## 九、文件清单

```
demo1/
├── .env                        ← 模型配置
├── backend/
│   ├── main.py                 ← FastAPI 入口
│   ├── config.py               ← 配置读取
│   ├── database.py             ← 数据库连接
│   ├── models.py               ← ORM 模型 + Pydantic 请求/响应
│   ├── security.py             ← 密码哈希 + JWT
│   ├── requirements.txt        ← Python 依赖
│   └── routers/
│       ├── auth.py             ← 注册/登录
│       ├── conversations.py    ← 会话 CRUD
│       └── messages.py         ← 消息发送（含 AI 调用）
└── frontend/
    ├── index.html              ← 唯一的页面
    ├── css/
    │   └── style.css           ← 全部样式
    └── js/
        ├── api.js              ← 后端 API 封装
        └── app.js              ← 页面逻辑
```

---

## 十、下一步可以学什么

1. **响应式布局** — 让手机也能正常用（`@media` 查询）
2. **流式输出** — AI 回复一个字一个字蹦出来（Server-Sent Events 或 WebSocket）
3. **组件化** — 把重复的 HTML 片段抽成 JS 函数生成
4. **前端框架** — Vue/React 本质上就是更系统化的 `state → render` 模式
