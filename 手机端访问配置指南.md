# 手机端访问配置指南

> 让手机也能打开你的 AI Fitness 助手。

---

## 一、为什么手机看不了？

项目原来是前后端分开的：

```
电脑:  双击 index.html 打开页面 → API 请求发到 127.0.0.1:8000
手机:  穿透工具只能暴露 8000 端口 → 访问 / 只看到一句 "API 运行中"
```

两个原因：
1. 前端页面文件（`index.html`）在电脑硬盘上，后端没托管它
2. API 地址写死了 `http://127.0.0.1:8000`，手机上的 `127.0.0.1` 指向手机自己

---

## 二、改了什么（3 处）

### `backend/main.py` — 让后端顺带托管前端文件

```python
from fastapi.staticfiles import StaticFiles
from fastapi.responses import FileResponse

# 把 frontend/css 和 frontend/js 挂载到后端
frontend_dir = Path(__file__).resolve().parent.parent / "frontend"
app.mount("/css", StaticFiles(directory=frontend_dir / "css"))
app.mount("/js", StaticFiles(directory=frontend_dir / "js"))

# 根路径返回前端页面，而不是 JSON
@app.get("/")
def read_root():
    return FileResponse(frontend_dir / "index.html")
```

### `frontend/js/api.js` — 去掉硬编码地址

```javascript
// ❌ 旧：写死 localhost，手机用不了
const API_BASE = 'http://127.0.0.1:8000';

// ✅ 新：空字符串 = 自动用当前域名
const API_BASE = '';
```

---

## 三、现在的工作方式

```
手机 → 公网隧道 → localhost:8000 (一个端口搞定一切)
                      ├─ /            → 返回前端页面
                      ├─ /css/*       → 样式文件
                      ├─ /js/*        → JS 文件
                      └─ /api/*       → 后端接口
```

---

## 四、怎么用

1. 启动后端（PyCharm 里点 ▶️）
2. 启动穿透工具（ngrok / frp / 花生壳），指向 `localhost:8000`
3. 手机浏览器打开穿透工具给的公网地址
4. 登录 → 聊天

---

## 五、记住一点

> **API 地址用相对路径，永远不要写死域名。**

`/api/xxx` 能自动适配任何环境——电脑、手机、公网、内网都通。写死 `localhost` 或 `127.0.0.1` 换环境就跪。

---

## 六、踩过的坑与修复

### 坑 1：侧边栏 280px 太宽，挤占了聊天区

手机屏幕窄（通常 375px），固定 280px 的侧边栏占了 3/4，右边聊天区只剩一点。

**修复**：加 `@media (max-width: 768px)` 移动端适配。

```css
/* style.css */
@media (max-width: 768px) {
    .sidebar {
        position: fixed; left: 0;
        transform: translateX(-100%);  /* 默认藏在屏幕左边外 */
        transition: transform 0.25s ease;
    }
    .sidebar.open { transform: translateX(0); }  /* 点 ☰ 滑出来 */
}
```

同时在 HTML 里加了顶部汉堡按钮 `<button id="btn-menu">☰</button>`，只在手机端显示。

### 坑 2：☰ 按钮点了没反应

点击 ☰ 后侧边栏闪现一下就缩回去了。

**原因**：点击事件冒泡（Bubble）。☰ 按钮在 `.chat-area` 里面，点击依次触发：
```
☰ 被点击 → openSidebar() 打开侧边栏 → 事件冒泡到 .chat-area
→ .chat-area 发现侧边栏开着 → closeSidebar() 关上
```
打开和关闭在几毫秒内连续发生，看起来就是"点了没反应"。

**修复**：阻止冒泡。

```javascript
// ❌
document.getElementById('btn-menu').addEventListener('click', openSidebar);

// ✅
document.getElementById('btn-menu').addEventListener('click', (e) => {
    e.stopPropagation();  // 阻止事件往上冒泡
    openSidebar();
});
```

### 坑 3：必须点具体会话才能收回侧边栏

用户习惯是点右侧空白区域就收回。

**修复**：给聊天区加点击监听。

```javascript
document.querySelector('.chat-area').addEventListener('click', (e) => {
    if (sidebar.classList.contains('open')) {
        closeSidebar();
    }
});
```

现在三种方式都能收：点 ☰ 再点一次、点遮罩暗色区域、点右侧聊天区。

### 坑 4：登录过一次后，每次打开网址都自动进聊天页

原来的启动逻辑检查了 `localStorage` 里的 token，有就跳过登录。

**修复**：强制每次打开都进登录页。

```javascript
// ❌ 旧：有 token 就跳过登录
if (state.token && state.user) {
    showAppPage();
}

// ✅ 新：每次进来都要登录
showAuthPage();
```

### 坑 5：手机注册的用户在哪？

手机 → 公网穿透 → 你电脑 `localhost:8000` → 同一个数据库。手机和电脑共享数据，注册的用户都在本地 MySQL 的 `users` 表里。

---

## 七、完整文件改动清单

| 文件 | 改了什么 |
|------|---------|
| `backend/main.py` | `/` 返回前端页面；挂载 `/css`、`/js` 静态目录 |
| `frontend/js/api.js` | `API_BASE` 从硬编码改为空字符串 |
| `frontend/css/style.css` | 加 `@media` 移动端适配、汉堡按钮、遮罩层、侧边栏滑入动画 |
| `frontend/index.html` | 加汉堡按钮、遮罩层 |
| `frontend/js/app.js` | 加侧边栏开关逻辑、`stopPropagation`、聊天区点击收回、强制每次登录 |
